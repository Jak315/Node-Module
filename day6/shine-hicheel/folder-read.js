const fs = require('fs')
const folderPath = '/Users/mtars_lab2_17/Desktop/node-modules-new/day6'
console.log(fs.readdirSync(folderPath));
fs.rename()

fs.rmdir(dir, { recursive: true }, (err) => {
    if err(){
        throw err;
    }
})

console.log();

